# Use either Ollama (local) or Groq (free cloud tier)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Optional Azure SQL connection string; if set, the app will prefer it over SQLite.
SQL_CONNECTION_STRING=
